# Joule runtime requirements — used by Joule build descriptor.
# Point your Joule build config at this file instead of requirements.txt.
#
# Includes litellm + langchain-litellm + sap-cloud-sdk (Joule LLM path).
# Excludes sap-ai-sdk-gen (conflicts with litellm's click pin).
# Excludes gunicorn + uvicorn[standard] (Joule uses its own process model).

litellm==1.86.1
langchain==1.2.15
langchain-core==1.3.3
langchain-litellm==0.3.5
langgraph==1.1.9
a2a-sdk[all]==0.3.22
fastapi!=0.136.3
uvicorn==0.40.0
httpx==0.28.1
python-dotenv==1.2.2
click==8.1.8
mcp>=1.0.0
opentelemetry-instrumentation-starlette
sap-cloud-sdk>=0.17.0
pyyaml>=6.0
hdbcli>=2.19.0
