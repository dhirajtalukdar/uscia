# Cloud Foundry requirements — used by CF Python buildpack.
# Joule runtime uses requirements-joule.txt instead.
#
# Removed (CF-incompatible):
#   litellm         -- pins click==8.1.8, conflicts with sap-ai-sdk-gen
#   langchain-litellm
#   sap-cloud-sdk   -- Joule/Kyma SDK, not available in CF
#   click==8.1.8    -- pinned by litellm; removed with litellm
#
# Added:
#   sap-ai-sdk-gen  -- provides gen_ai_hub for AI Core LLM init on CF
#   gunicorn        -- CF start command
#   uvicorn[standard] -- ASGI worker

langchain==1.2.15
langchain-core==1.3.3
langgraph==1.1.9
a2a-sdk[all]==0.3.22
fastapi!=0.136.3
uvicorn[standard]==0.40.0
httpx==0.28.1
python-dotenv==1.2.2
mcp>=1.0.0
opentelemetry-instrumentation-starlette
pyyaml>=6.0
hdbcli>=2.19.0
sap-ai-sdk-gen==6.6.0
gunicorn~=22.0
